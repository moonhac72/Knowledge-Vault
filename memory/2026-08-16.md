# 2026-08-16 일일 메모리

- **로컬 LLM (Ollama) 환경 점검 및 연동 완료**:
  - `antigravity.config.json` 설정 점검 및 모델명을 `gemma4:e4b`로 정정.
  - `http://localhost:11434/v1` 엔드포인트 통신 및 챗 컴플리션 테스트 성공 (HTTP 200).
- **워크플로우 규칙 등록**:
  - **상시 모델 선택**: 아무리 간단한 단문 질문이라도 항상 먼저 `ask_question` 선택 모달로 **Gemma 4 (로컬)** vs **Gemini 3.7 Flash (클라우드)**를 선택받은 후 진행.
  - **문서화 저장 트리거 (`/저장해줘`)**: 프롬프트 끝에 `/저장해줘`가 포함된 경우에만 지식 창고/인사이트 융합 카테고리에 마크다운 파일로 저장하고, 그 외에는 파일 생성 없이 채팅창으로만 응답.
  - `TOOLS.md`, `memory/MEMORY.md`, `memory/local_model_workflow.md`에 최종 반영 완료.
